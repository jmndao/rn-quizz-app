import { Text, View } from "react-native";
import React from "react";

const DetailScore = ({ answers }) => {
  return (
    <View>
      <Text>DetailScore</Text>
    </View>
  );
};

export default DetailScore;
